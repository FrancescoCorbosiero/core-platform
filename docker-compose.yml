services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - '5432:5432'
    environment:
      POSTGRES_DB: tereso
      POSTGRES_USER: tereso
      POSTGRES_PASSWORD: tereso
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
